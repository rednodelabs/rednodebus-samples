# Copyright (c) 2020 RedNodeLabs

set(REDNODELABS_DIR ${CMAKE_CURRENT_LIST_DIR} CACHE PATH "RedNodeLabs root directory")

if(NOT DEFINED REDNODEBUS_LIB_IMPORT)
    # zephyr_include_directories(include)

    # add_subdirectory(drivers)
endif()
